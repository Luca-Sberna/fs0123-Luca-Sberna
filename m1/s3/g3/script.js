//calcolo distanza tra due punti 
function distanceBetweenPoints(a, b) {
    a = [9, 2];
    b = [1, 8];
    const distance = Math.sqrt((a[0] - b[0]) ** 2 + (a[1] - b[1]) ** 2);


    return document.write(distance);
}

//----------------------------------------------------------------


